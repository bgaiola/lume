{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the Lume desktop main window in Phase 2 Block 1. Kept intentionally minimal: only core IPC + the log plugin. Native capture and input control add their own capabilities in Block 2.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "log:default"
  ]
}
