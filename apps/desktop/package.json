{
  "name": "@lume/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Lume desktop technician panel (Tauri 2 shell around apps/web).",
  "type": "module",
  "scripts": {
    "_comment": "Cargo must be on PATH. macOS dev: ensure ~/.cargo/bin is in PATH (rustup default). CI: dtolnay/rust-toolchain adds it automatically.",
    "dev": "tauri dev",
    "build": "cargo build --manifest-path src-tauri/Cargo.toml",
    "bundle": "tauri build",
    "bundle:debug": "tauri build --debug",
    "tauri": "tauri",
    "lint": "echo 'apps/desktop has no JS sources to lint (Rust handled by cargo)' && exit 0",
    "typecheck": "echo 'apps/desktop has no JS sources to typecheck (Rust handled by cargo check)' && exit 0",
    "test": "echo 'no tests yet' && exit 0",
    "clean": "rm -rf src-tauri/target src-tauri/gen .turbo"
  },
  "dependencies": {
    "@lume/web": "workspace:*"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
